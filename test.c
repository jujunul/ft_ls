#include <unistd.h>
#include "ls.h"

void	ft_aff(char **tab)
{
	int i;
	while
}

int main(void)
{
	char *str;
	str = (char *)malloc(100000);
	str = "salut les amis\0";
	ft_aff(ft_split_whitespaces(str));
}